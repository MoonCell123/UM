%========================================%
%           Custom Macros                   %
%========================================%

% --- Math notation ---
\newcommand{\loss}{\mathcal{L}}
\newcommand{\dataset}{\mathcal{D}}
\newcommand{\model}{\mathcal{M}}
\newcommand{\param}{\theta}
\newcommand{\real}{\mathbb{R}}
\newcommand{\expect}{\mathbb{E}}
\newcommand{\prob}{\mathbb{P}}

% --- Shorthands ---
\newcommand{\argmax}{\arg\max}
\newcommand{\argmin}{\arg\min}
\newcommand{\softmax}{\mathrm{softmax}}
\newcommand{\sigmoid}{\sigma}

% --- Vector / matrix ---
\newcommand{\vect}[1]{\mathbf{#1}}          % \vect{x} → bold vector
\newcommand{\mat}[1]{\mathbf{#1}}           % \mat{W} → bold matrix
\newcommand{\tsp}[1]{#1^{\top}}             % \tsp{x} → x^⊤

% --- Sets ---
\newcommand{\set}[1]{\{#1\}}               % \set{x} → {x}

% --- Redefined symbols for readability ---
\renewcommand{\hat}[1]{\widehat{#1}}       % wider hats
\renewcommand{\tilde}[1]{\widetilde{#1}}   % wider tildes
