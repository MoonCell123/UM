\section{Conclusion}
In this paper, we propose [Method Name], an interpretable plug-and-play representation fusion module that decomposes foundation model fusion into individual contribution estimation and pairwise relationship modeling.
Specifically, we first employ intervention-based attribution to quantify the sample-specific contribution of each foundation model representation.
We then extend the intervention paradigm to pairwise intervention attribution and introduce pairwise interaction attribution (PIA) to characterize cross-model relationships.
Finally, we leverage these relational estimates to construct an interaction-aware joint representation, enabling the fused embedding to capture cross-model relational information.
Additionally, we develop a semantic prior-guided module that incorporates global semantic references to stabilize contribution estimation and improve robustness.
Experiments and ablation studies on [datasets] datasets demonstrate that our method achieves competitive performance compared with existing fusion strategies.
In the future, we will investigate more scalable relational modeling strategies for large-scale foundation model collections and explore whether the discovered cross-model relationships can provide further insights into foundation model specialization.